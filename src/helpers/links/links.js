import paths from "../paths/paths";

const links = [
    {
        label: "Home", 
        href: paths.home
    },
    {
        label: "Redux", 
        href: paths.redux
    }
]

export default links